# 🛠️ Cypress Test Automation

This project uses **Cypress** for end-to-end testing. Below are the essential commands to set up, run, and debug tests.

---

## 🚀 Installation

Before running Cypress, install the required dependencies:

npm install
